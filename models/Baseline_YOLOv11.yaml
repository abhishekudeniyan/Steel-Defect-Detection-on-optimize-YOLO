# Ultralytics YOLOv11n Baseline Architecture
nc: 80 # Default COCO classes

scales:
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024]

# =========================
# BACKBONE
# =========================
backbone:
  # Stem
  - [-1, 1, Conv, [64, 3, 2]]          # 0-P1/2

  # P2
  - [-1, 1, Conv, [128, 3, 2]]         # 1-P2/4
  - [-1, 2, C3k2, [128, False, 0.25]]  # 2 (c3k=False for nano scale)

  # P3
  - [-1, 1, Conv, [256, 3, 2]]         # 3-P3/8
  - [-1, 2, C3k2, [256, False, 0.25]]  # 4-P3 Feature Map

  # P4
  - [-1, 1, Conv, [512, 3, 2]]         # 5-P4/16
  - [-1, 2, C3k2, [512, True]]         # 6-P4 Feature Map (c3k=True)

  # P5
  - [-1, 1, Conv, [1024, 3, 2]]        # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]        # 8

  # Attention & Pooling
  - [-1, 1, SPPF, [1024, 5]]          # 9
  - [-1, 1, C2PSA, [1024]]            # 10-P5 Top Feature Map


# =========================
# HEAD (PAN-FPN)
# =========================
head:
  # P5 -> P4
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 11
  - [[-1, 6], 1, Concat, [1]]                  # 12
  - [-1, 2, C3k2, [512, False]]                # 13

  # P4 -> P3
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 14
  - [[-1, 4], 1, Concat, [1]]                  # 15
  - [-1, 2, C3k2, [256, False]]                # 16 (P3 Output)

  # P3 -> P4
  - [-1, 1, Conv, [256, 3, 2]]               # 17
  - [[-1, 13], 1, Concat, [1]]                 # 18
  - [-1, 2, C3k2, [512, False]]                # 19 (P4 Output)

  # P4 -> P5
  - [-1, 1, Conv, [512, 3, 2]]               # 20
  - [[-1, 10], 1, Concat, [1]]                 # 21
  - [-1, 2, C3k2, [1024, True]]                # 22 (P5 Output)

  # Detect Head
  - [[16, 19, 22], 1, Detect, [nc]]            # 23