model: models\Baselineyolov8nano.yaml

training:
  imgsz: 480
  batch: 6
  epochs: 150
  optimizer: AdamW
  lr0: 0.001
  mosaic: 0
  patience: 20
  workers: 2
  amp: true
  cos_lr: true
  warmup_epochs: 5
     # freeze backbone layers


experiment:
  name: Baseline__YOLO_v8n_480px_AdamW_cos_lr_150epochs
  seeds: [0,1,2]
  project_dir: runs/detect/experiments

data:
  config: configs/data.yaml